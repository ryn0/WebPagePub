@model WebPagePub.Web.Models.StructedDataWebsiteModel
@using Newtonsoft.Json;
 

@if (Model != null)
{
<script type="application/ld+json">
    
    @Html.Raw(JsonConvert.SerializeObject(Model))

    </script>
 
 
}