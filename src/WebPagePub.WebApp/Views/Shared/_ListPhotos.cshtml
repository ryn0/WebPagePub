@model List<WebPagePub.Web.Models.SitePagePhotoModel>


@if (Model.Count > 0)
{
    <ul>
        @foreach (var item in Model)
        {
            <li>
                 <img src="@item.PhotoThumbUrl" />
            </li>
        }
    </ul>

    
}
 
    <hr />