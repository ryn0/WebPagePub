@model StructedDataWebsiteModel
@using Newtonsoft.Json;
@using WebPagePub.WebApp.Models.StructuredData;
 

@if (Model != null)
{
<script type="application/ld+json">
    
    @Html.Raw(JsonConvert.SerializeObject(Model))

    </script>
 
 
}