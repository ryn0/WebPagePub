@model List< WebPagePub.WebApp.Models.SitePage.SitePageCommentDisplayModel>


@if (Model.Count > 0)
{
    <ul>
        @foreach (var item in Model)
        {
            <li>
                @if (string.IsNullOrWhiteSpace(item.Website))
                {
                    <b>@item.Name</b>
                }
                else
                {
                    <a href="@item.Website" rel="nofollow">@item.Name</a>
                }
                <i>@item.FriendlyCreateDateDisplay</i>
                <br />
                <p>
                    @item.Comment
                </p>
            </li>
        }
    </ul>

    
}
else
{
    <i>No Comments</i>
    <br />
}

    <hr />