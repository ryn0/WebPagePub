For the given keyword/ phrase: "[keyword]" which you just wrote an article on, create an SEO optimized <h1> tag.

Do not response with HTML. Just provide the text which would go into an <h1></h1> tag.